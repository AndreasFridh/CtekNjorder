# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Local add-on options (may contain a broker password)
options.json
sim-options.json
*-options.json

# Runtime logs
*.log

# Real MQTT captures from a live installation - contain device serials and
# household consumption data. Regenerate your own with tools/sniff.py.
captures/

# Raw vendor artwork. The derived icon.png / logo.png are committed; this is
# only needed to regenerate them (tools/make_icons.py).
branding/source.*
